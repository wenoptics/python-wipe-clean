[tool.poetry]
name = "python-wipe-clean"
version = "0.1.0"
description = ""
authors = ["wenoptk <wenoptics@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"
rich = "^11.2.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
